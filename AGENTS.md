# AGENTS.md

## CHANGELOG Policy (Strict)
- Only edit content under `## Unreleased` in `CHANGELOG.md`.
- Never modify versioned sections like `## 0.x.y`.
- Never reorder, rewrite, or clean up historical changelog entries.
- If a requested change appears to require edits outside `## Unreleased`, ask for explicit approval first.
- If `## Unreleased` is missing, add it at the top and only write there.
