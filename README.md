// CLI flag description for howdoi
// Usage: howdoi -p "check for current kubernetes namespace"

// Adds a short `-p`/`--prompt` flag using clap 4.
// The app will only read the flag value; if omitted it falls back to stdin.
